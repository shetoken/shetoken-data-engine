# ════════════════════════════════════════════════════════════
# SHEtoken WEI Agent — Environment Variables
# Save this file as:  shetoken_repo/agent/.env
# NEVER commit this file to GitHub (it's in .gitignore)
# ════════════════════════════════════════════════════════════


# ── GMAIL (required — sends your weekly newsletter) ──────────
# Your Gmail address
GMAIL_USER=techdiva.ai@gmail.com

# App Password (NOT your regular Gmail password)
# Get it at: myaccount.google.com/apppasswords
# Steps: Google Account → Security → 2-Step Verification → App passwords
# Select "Mail" + "Windows Computer" → Copy the 16-character password
GMAIL_APP_PASSWORD=wpfw kcbt gnue mmvj

# Who receives the founder (full internal) version
REPORT_TO_EMAIL=techdiva.ai@gmail.com

# Subscribers get the clean newsletter version (comma-separated)
# Leave blank if none yet
NEWSLETTER_SUBSCRIBERS=Ayindri.b@gmail.com

# NGO partners get NGO-focused version (comma-separated)
# Example: kudumbashree@kerala.gov.in,jeevika@bihar.gov.in
NEWSLETTER_NGOS=


# ── GOOGLE SHEETS (optional — weekly data dashboard) ─────────
# Create a sheet at sheets.google.com, copy the ID from the URL:
# https://docs.google.com/spreadsheets/d/THIS_IS_THE_ID/edit
GOOGLE_SHEET_ID=

# Service account JSON key (for writing to Sheets)
# Get it at: console.cloud.google.com
# Steps: IAM & Admin → Service Accounts → Create → Keys → JSON
# Copy the entire JSON content as one line, or paste the file path
GOOGLE_SERVICE_ACCOUNT_JSON=


# ── YOUTUBE DATA API (optional — scans YouTube for WEI signals) ──
# IMPORTANT: Generate a NEW key — your previous key was shared publicly
# Get it at: console.cloud.google.com
# Steps: APIs & Services → Library → Enable "YouTube Data API v3"
#        → Credentials → Create API Key → Restrict to YouTube Data API v3
YOUTUBE_API_KEY=AIzaSyA6FdVhaTeYnkF9OwtDGWK6gIFt4ZszC6M


# ── TWITTER / X (optional — posts weekly thread to @ShetokenDAO) ─
# Get all 4 keys at: developer.twitter.com
# Steps: Create Project → Create App → Settings → User authentication
#        → Read + Write → Keys and Tokens → Generate all 4
TWITTER_API_KEY=
TWITTER_API_SECRET=
TWITTER_ACCESS_TOKEN=
TWITTER_ACCESS_TOKEN_SECRET=


# ── INSTAGRAM (optional — posts weekly branded image) ────────
# Requires: Instagram Business account + Facebook Developer app
# Get it at: developers.facebook.com
# Steps: Create App → Instagram Graph API → Generate long-lived token
IG_ACCESS_TOKEN=
IG_BUSINESS_ID=

# ImgBB — free image hosting (Instagram needs a public image URL)
# Get it at: imgbb.com → API → Get free API key
IMGBB_API_KEY=


# ── SUPABASE (optional — permanent API call tracking) ────────
# Get it at: supabase.com → New Project (free tier)
# Steps: Settings → API → copy URL and anon key
# Then create the api_calls table (SQL in api/README_ANALYTICS.md)
SUPABASE_URL=
SUPABASE_ANON_KEY=


# ── API ANALYTICS BACKEND ─────────────────────────────────────
# memory   = in-memory only, resets on redeploy (default, works immediately)
# supabase = permanent storage (fill in Supabase keys above first)
# file     = saves to analytics_log.jsonl locally
ANALYTICS_BACKEND=memory


# ── OLLAMA (SLM runtime — runs locally on your machine) ──────
# Default is localhost. Only change if running Ollama on a different machine.
# Make sure Ollama is running: ollama serve
# Make sure models are pulled: ollama pull phi3.5 && ollama pull qwen2.5:3b
OLLAMA_HOST=http://localhost:11434


# ── GOOGLE CLOUD RUN (optional — for automated Sunday scheduling) ─
# Only needed if deploying agent to Google Cloud Run
# Get project ID at: console.cloud.google.com
GCP_PROJECT_ID=
GCP_REGION=us-central1


# ════════════════════════════════════════════════════════════
# WHAT YOU ACTUALLY NEED ON DAY ONE:
#
#   REQUIRED (agent won't run without these):
#     GMAIL_USER
#     GMAIL_APP_PASSWORD
#     REPORT_TO_EMAIL
#     OLLAMA_HOST (default is fine)
#
#   RECOMMENDED (add these in week 1):
#     YOUTUBE_API_KEY    ← new key only
#     GOOGLE_SHEET_ID
#     GOOGLE_SERVICE_ACCOUNT_JSON
#
#   OPTIONAL (add when ready):
#     Twitter keys       ← when @ShetokenDAO account is set up
#     Instagram keys     ← when Instagram Business account is set up
#     Supabase keys      ← when you want permanent analytics
#     GCP keys           ← when deploying to Cloud Run
# ════════════════════════════════════════════════════════════